start:
TextWindow.WriteLine("hello my name is ros the rubbish operating system")
TextWindow.write("enter your object: ")
name = TextWindow.Read()

If name = ("plastic") Then 
  TextWindow.WriteLine("plastic is recyclable")
  Goto start
EndIf


If name = ("cardboard") Then
  TextWindow.WriteLine("cardboard is recyclable")
Goto start
EndIf 

If name = ("can") Then
  TextWindow.WriteLine("a can is recyclable")
  Goto start
EndIf

If name = ("book") Then
  TextWindow.WriteLine("a book is recyclable")
  Goto start
EndIf

If name = ("cat") Then
  TextWindow.WriteLine("why would you throw out A CAT!!!!!!!!!!!!")
  Goto start
EndIf

If name = ("paper") Then
  TextWindow.WriteLine("paper is recyclable")
  Goto start
EndIf
